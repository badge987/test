spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crud_backend_rest_1
    username: postgres
    password: mounir123

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop

server:
  port: 8081

enova:
  app:
    jwtSecret: "enovaenovaenovaenovaenovaenovaenovaenovaenovaenovaenovaenovaenov"
    jwtExpirationMs: 86400000